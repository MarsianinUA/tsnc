// `<`, `<=`, `>` and `>=` order two numbers or two strings. Anything else has no order to read.
// Requirements 3.7 keeps the comparisons as strict as the equalities.
// expect: T3005 4:15
const later = true < false;
