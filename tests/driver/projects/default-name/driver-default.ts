// Named for the one test that builds it: without `-out:` the artifact takes this file's stem, in
// the working directory, so the file it leaves there for a moment is recognisably a test's own.

console.log("ok");
